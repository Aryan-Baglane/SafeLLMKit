pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "SafeLLMKit"
include(":safellmkit-core")
include(":safellmkit-ml")
